# E2E tests (scaffolded)

Playwright end-to-end flows belong here (`@playwright/test` is already
a devDependency). Not yet implemented — see the README "Known
limitations". Suggested first flows, per the master spec:

1. Home → Text Art → generate → copy
2. Mosaic → upload image → generate → download
3. Admin → login → approve pattern → feature pattern
